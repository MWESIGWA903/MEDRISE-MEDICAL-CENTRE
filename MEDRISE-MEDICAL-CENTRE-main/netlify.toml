[build]
base = "MEDRISE-MEDICAL-CENTRE-main"
command = "pnpm install && pnpm run build"
publish = "artifacts/medrise/dist/public"

[build.environment]
NODE_VERSION = "20"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200