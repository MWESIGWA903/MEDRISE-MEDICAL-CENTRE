- [MedRise zod-import rule](medrise-zod-import.md) — all schema files must use `from "zod"` not `from "zod/v4"`; workspace is Zod v3
- [MedRise workflows](medrise-workflows.md) — API on port 8080, frontend on port 8081; workflow names are "Start API" and "Start Frontend"
- [MedRise drizzle-zod compatibility](medrise-drizzle-zod.md) — drizzle-zod v0.8.x is type-incompatible with Zod v3; lib/db needs noEmitOnError:false in tsconfig to emit declarations despite errors
- [MedRise auth design](medrise-auth-design.md) — owner/admin can access both portals; staff login allows all roles; force-pwd-change uses localStorage key medrise_must_change_pwd
- [MedRise db & auth state](medrise-db-auth-state.md) — 17 tables in DB (added ward_round_notes, inpatient_drug_chart, nursing_notes); passwords bcrypt-hashed (cost 12); default admin Hannington/medical_director seeded
- [MedRise admissions module](medrise-admissions.md) — admissions tab now CLINICAL_ROLES; click active patient → InpatientDetail (ward rounds/drug chart/nursing notes); protocols tab added for CLINICAL_ROLES with BookOpen icon
- [MedRise WebSocket notifications](medrise-ws-notifications.md) — WS on port 8080 /ws, auth via ?token= query param; Vite proxies /ws without base path prefix; notifications+dismissals tables in DB
- [MedRise QueueStatus types](medrise-queue-status.md) — "theatre" added to QueueStatus, STATUS_CONFIG, NEXT_LABEL; must also add to QueueEntryUpdateStatus in both lib/api-zod and lib/api-client-react (generated files); handleStatusChange uses `as any` cast for QueueStatus→QueueEntryUpdateStatus
- [MedRise inpatient module](medrise-inpatient.md) — inpatient routes at /api/inpatient/*; mobile: getApiToken (not getToken), getApiBaseUrl from lib/baseUrl.ts; wards tab added to both NativeTabs and ClassicTabs layouts
- [MedRise mobile security](medrise-mobile-security.md) — AuthContext uses SecureStore (not AsyncStorage) for token; falls back to AsyncStorage on web (Platform.OS==="web"); vitals.tsx has clinical range validation (validateVitals); patients.tsx has PAGE_SIZE=50 pagination
- [MedRise Expo workflow fix](medrise-expo-workflow.md) — artifact service runs dev command from within artifact dir; use `pnpm run dev` not `pnpm --filter @workspace/... run dev`; PORT must default: use ${PORT:-20537}
- [MedRise web artifact registration](medrise-web-artifact.md) — web app (artifacts/medrise) is not registered in artifact system; verifyAndReplaceArtifactToml fails (schema mismatch); app IS accessible at port 8081 → external port 80; Start Frontend workflow has outputType=webview
- [MedRise Render deployment](medrise-render-deploy.md) — API uses pre-built dist bundle (echo build cmd); push via GITHUB_TOKEN with repo+workflow scopes; SPA routing via Render dashboard Rewrite rule not _redirects file
